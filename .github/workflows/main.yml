name: Log action
on: 
  label:
    types: created
  issues:
    types: opened

jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - name: Print info      
        run: echo "Trigger ${{ github.event_name }}, User ${{ github.actor }}, date - `date`"

